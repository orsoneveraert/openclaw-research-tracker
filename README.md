# OpenClaw Research Tracker

Public static dashboard for tracking OpenClaw research progress.

This repository contains the published tracker only:

- `index.html`
- `styles.css`
- `app.js`
- `data.json`

The source workspace remains private in `openclaw-workspace`.
